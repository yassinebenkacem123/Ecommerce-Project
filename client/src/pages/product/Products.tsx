import Template from "../../utils/Template";
const Products = () => {
  return (
    <Template>
        <section className="h-screen">
            Products
        </section>

    </Template>
  )
}

export default Products